---
title: "Sklep  rowerowy"
author: "MG, PO, BK"
date: "2025-11-03"
output: html_document
---

```{r setup, include=FALSE}
# Instalacja pakietów:
# Lista wymaganych pakietów:
packages <- c("knitr", "visdat", "naniar", "ggplot2", "dplyr", "tidyr",
              "stringr", "janitor", "mice", "finalfit", "kableExtra", "hms", "rlang")

# Instalacja brakujących pakietów:
installed <- packages %in% installed.packages()
if(any(!installed)) {
  install.packages(packages[!installed], dependencies = TRUE)
}

# Ładowanie bibliotek
for (pkg in packages) {
  library(pkg, character.only = TRUE)
}

# Rekomendowane ustawienie opcji knitr w RMarkdown
knitr::opts_chunk$set(echo = TRUE)


dane <- read.csv("sklep_rowerowy.csv", header = TRUE, sep = ",")
head(dane)
```

# Wstęp

Projekt **Sklep rowerowy** to analiza danych sklepu rowerowego na potrzeby przedmiotu: Analiza danych w R.\
Celem jest badanie zależności między cechami klientów a decyzją o zakupie roweru. Analiza obejmuje przetwarzanie danych, imputację braków oraz modelowanie statystyczne.

```{r wczytanie danych}

dane <- read.csv("sklep_rowerowy.csv", header = TRUE, sep = ",")
head(dane)
```

#Pytania badawcze:
###Jakie cechy klientów (wiek, dochód, region itd.) mają największy wpływ na decyzję o zakupie roweru?
###Struktura danych i analiza braków
###Czy posiadanie samochodu lub własnego domu wpływa na prawdopodobieństwo zakupu roweru?
###Czy styl życia (dojazd do pracy, status zawodowy, wykształcenie) koreluje z zakupem roweru?
###Czy są różnice w decyzji zakupu roweru między klientami w różnych regionach lub grupach wiekowych?

## Nazwy kolumn w zbiorze danych:

```{r struktura danych}
library(knitr)
library(kableExtra)
kable(colnames(dane), caption = "Lista nazw kolumn w zbiorze danych") %>%
  kableExtra::kable_styling(font_size = 16, bootstrap_options = "striped") %>%
  kableExtra::row_spec(0, bold = TRUE, font_size = 18)



```

## Liczba brakujących wartości, proporcje i kompletność danych:

```{r Liczba brakujących wartości, proporcje i kompletność}
library(naniar)

cat("Liczba braków w danych:", n_miss(dane), "\n")
cat("Liczba kompletnych obserwacji:", n_complete(dane), "\n")
cat("Proporcja braków:", round(prop_miss(dane)*100, 2), "%\n")

```

## Procent brakujących wartości na zmienną:

```{r Procent brakujących wartości na zmienną}
pct <- pct_miss(dane)
print(pct)

```

## Podsumowanie braków na poziomie zmiennych i obserwacji:

```{r Podsumowanie braków na poziomie zmiennych i obserwacji}
miss_var_summary(dane)
miss_case_table(dane)

```

## Wizualizacji braków danych z użyciem pakietu visdat

```{r wykresy NA}
wykres1 <- vis_dat(dane) +
  theme(axis.text.x = element_text(hjust = 1, margin = margin(t = 10))) +
  ggtitle("Wizualizacja danych z użyciem visdat") +
  theme(plot.title = element_text(hjust = 0.5, size = 16, face = "bold"))

wykres2 <- vis_miss(dane) +
  theme(axis.text.x = element_text(hjust = 1, margin = margin(t = 10))) +
  ggtitle("Liczba brakujących wartości w zmiennych") +
  theme(plot.title = element_text(hjust = 0.5, size = 16, face = "bold"))
print(wykres1)
print(wykres2)

```

# Badanie wzorców braków danych

```{r wzorce braków danych}

missing_pattern(dane)
```

# Imputacja brakujących danych

## Przygotowanie danych do imputacji

```{r przygotowanie danych do imputacji}
dane <- dane %>% janitor::clean_names() %>% select(-id)

summary(dane)
colSums(is.na(dane))

dane <- dane %>% mutate(across(where(is.character), ~ na_if(., "")))

dane$marital_status <- as.factor(dane$marital_status)
dane$gender <- as.factor(dane$gender)
dane$home_owner <- as.factor(dane$home_owner)
colSums(is.na(dane))

```

## Imputacja danych z użyciem dominanty

### rozkład miennnych kategorycznych przed imputacją

```{r imputacja danych}
cat("Rozkład Marital Status przed imputacją:\n")
print(table(dane$marital_status))

cat("Rozkład Gender przed imputacją:\n")
print(table(dane$gender))

cat("Rozkład Home Owner przed imputacją:\n")
print(table(dane$home_owner))
```

## Imputacja danych

```{r imputacja danych}
# Przykład dla jednej zmiennej kategorycznej
dominanta <- names(sort(table(dane$marital_status), decreasing = TRUE))[1]
dane$marital_status[is.na(dane$marital_status)] <- dominanta
dominanta_gender <- names(sort(table(dane$gender), decreasing = TRUE))[1]
dane$gender[is.na(dane$gender)] <- dominanta_gender
dominanta_home_owner <- names(sort(table(dane$home_owner), decreasing = TRUE))[1]
dane$home_owner[is.na(dane$home_owner)] <- dominanta_home_owner
```

## imputacja danych z użyciem średniej

```{r imputacja danych z użyciem miediany}
dane2<- as.data.frame(dane)

dane2$cars[is.na(dane2$cars)] <- median(dane2$cars, na.rm = TRUE)
dane2$age[is.na(dane2$age)] <- median(dane2$age, na.rm = TRUE)
dane2$children[is.na(dane2$children)] <- median(dane2$children, na.rm = TRUE)
if (!require(VIM)) install.packages("VIM")
library(VIM)

dane2 <- hotdeck(dane2, variable = "income")

missing_pattern(dane2)

```

#2.2 Walidacja danych
#sprawdzam czy są duplikaty w zbiorze danych:
```{r walidacja danych}
# Najpierw zainstaluj i załaduj pakiet:
if (!require(validate)) install.packages("validate")
library(validate)

rules <- validator(
  cars == as.integer(cars),
  children == as.integer(children),
  age == as.integer(age),
  children >= 0,
  income >= 0,
  cars >= 0,
  age >= 0,
  commute_distance >= 0,
  home_owner %in% c("Yes", "No"),
  purchased_bike %in% c("Yes", "No"),
  marital_status %in% c("Married", "Single"),
  gender %in% c("Male", "Female"),
  education %in% c("Bachelors", "Partial College", "High School", "Partial High School", "Graduate Degree"),
  occupation %in% c("Skilled Manual", "Clerical", "Professional", "Manual", "Management"),
  region %in% c("Europe", "Pacific", "North America")
)

out <- confront(dane2, rules)
summary(out)
```

sum(duplicated(dane2))

#WIZUALIZACJA DANYCH
```{r wizualizacja danych}
#income one variable  plot
ggplot(dane2, aes(x = income)) +
  geom_histogram(binwidth = 5000, fill = "green", color = "black") +
  labs(title = "Rozkład dochodów klientów", x = "Dochód", y = "Liczba klientów")


```
